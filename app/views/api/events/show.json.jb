{
  id: @event.id,
  title: @event.title,
  description: @event.description,
  date: @event.date,
  time: @event.time,
  location: @event.location,
  image: @event.image,
  user_id: @event.user_id,
  users: @event.users,
}
